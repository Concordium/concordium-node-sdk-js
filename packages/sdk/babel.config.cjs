/**
 * Used for react-native tests configured in `jest.config.react-native.ts`
 */

/** @type {import('ts-jest').JestConfigWithTsJest} */
module.exports = {
    presets: ['module:metro-react-native-babel-preset'],
};
